arch: amd64          # optional, this is default, routes to a full VM
os: linux            # optional, this is default
dist: bionic          # or bionic | xenial | trusty | precise with xenial as default

language: node_js

node_js: 
  - node



script:
  - npm lint
  - npm test
